apiVersion: certmanager.k8s.io/v1alpha1
kind: Certificate
metadata:
  name: minienv-staging-certificate
  namespace: default
spec:
  secretName: minienv-staging-tls-secret
  issuerRef:
    name: minienv-letsencrypt-staging-issuer
  commonName: "*gke.minienv.com"
  dnsNames:
  - gke.minienv.com
  acme:
    config:
    - dns01:
        provider: clouddns
      domains:
      - "*gke.minienv.com"
      - gke.minienv.com