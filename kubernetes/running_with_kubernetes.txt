minikube start

# Namespace
kubectl create -f ./namespace.yml

# Registry
kubectl create -f ./daemonset-registry.yml

# API
kubectl create -f ./<HOST>/deployment-api.yml
kubectl create -f ./service-api.yml

# Web
kubectl create -f ./<HOST>/deployment-web.yml
kubectl create -f ./service-web.yml

# Run a single deployment & service at runtime for each user/docker-compose sample
kubectl create -f ./examples/deployment-example-1.yml --namespace=minienv
kubectl create -f ./examples/service-example-1.yml --namespace=minienv

# DNS *.minikube.dev points to 192.168.100.99

# File Browser
http://minikube.dev:NODE_IP_MAPPED_TO_33001

# docker-compose container URLS
http://port-exposed-through-dockercompose:minikube.dev:NODE_IP_MAPPED_TO_33002
http://33000.minikube.dev:31018
