minikube addons disable default-storageclass
minikube start

# Registry
kubectl create -f ./pv-dreg.yml
> ssh into kubernetes node and add config.yml to /data
kubectl create -f ./pvc-dreg.yml
kubectl create -f ./pod-dreg.yml
kubectl create -f ./svc-dreg-clusterip.yml
kubectl create -f ./svc-dreg-nodeport.yml

# Run a single pod & service at runtime for each user/docker-compose sample
kubectl create -f ./pod-dind-1.yml
kubectl create -f ./svc-dind-1.yml

# DNS *.minikube.dev points to 192.168.100.99

# File Browser
http://minikube.dev:NODE_IP_MAPPED_TO_33001

# docker-compose container URLS
http://port-exposed-through-dockercompose:minikube.dev:NODE_IP_MAPPED_TO_33002
http://33000.minikube.dev:31018
