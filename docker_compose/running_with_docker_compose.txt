# Run minienv
# Set git repo and run docker-compose up with optional name (-p)
EXUP_GIT_REPO=<GIT_REPO> docker-compose -p <NAME> up
EXUP_GIT_REPO=https://github.com/markwatsonatx/tutorial-rethinkdb-nodejs-rps docker-compose -p example1 up

# Access log and editor with assigned port
http://localhost:33001

# Access editor
http://localhost:33002

# docker-compose container URLS
http://port-exposed-through-dockercompose.localhost:33002
http://33000.localhost:33002
